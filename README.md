# QR Code Generator

A simple Python-based QR Code Generator that converts a URL into a QR code image.

## Features

- Generate QR codes from URLs
- Save QR codes as PNG images
- Automatically adds `.png` extension if not provided
- Simple command-line interface

## Technologies Used

- Python
- qrcode library

## Installation

Install the required library:

```bash
pip install qrcode